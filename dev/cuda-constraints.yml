# Mixin environment file for the conda-lock process generate a Cuda
# environment (on Linux, other platforms are still CPU).
dependencies:
  - pytorch-gpu # [linux]
  - pytorch-cpu # [osx]
  - pytorch-mutex=*=cpu # [win]
  - libblas=*=*mkl* # [linux]
